[pytest]
DJANGO_SETTINGS_MODULE = uk_grid_site.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    integration: marks tests that require live external services (Snowflake, APIs)
    slow: marks tests that are notably slow
