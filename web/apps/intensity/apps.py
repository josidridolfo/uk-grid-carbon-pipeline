from django.apps import AppConfig


class IntensityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.intensity"
    label = "intensity"
    verbose_name = "Carbon Intensity"
